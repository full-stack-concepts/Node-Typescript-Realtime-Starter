export interface ICustomLevels { 
	levels: {
		error: number, 
  		warn: number, 
  		info: number, 
  		verbose: number, 
  		debug: number, 
  		application: number,
  		access: number 
	}
};